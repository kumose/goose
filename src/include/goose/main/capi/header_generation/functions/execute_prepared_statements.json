{
    "group": "execute_prepared_statements",
    "deprecated": false,
    "description": "Functions to execute a prepared statement.",
    "entries": [
        {
            "name": "goose_execute_prepared",
            "return_type": "goose_state",
            "params": [
                {
                    "type": "goose_prepared_statement",
                    "name": "prepared_statement"
                },
                {
                    "type": "goose_result *",
                    "name": "out_result"
                }
            ],
            "comment": {
                "description": "Executes the prepared statement with the given bound parameters, and returns a materialized query result.\n\nThis method can be called multiple times for each prepared statement, and the parameters can be modified\nbetween calls to this function.\n\nNote that the result must be freed with `goose_destroy_result`.\n\n",
                "param_comments": {
                    "prepared_statement": "The prepared statement to execute.",
                    "out_result": "The query result."
                },
                "return_value": "`GooseSuccess` on success or `GooseError` on failure."
            }
        },
        {
            "name": "goose_execute_prepared_streaming",
            "return_type": "goose_state",
            "params": [
                {
                    "type": "goose_prepared_statement",
                    "name": "prepared_statement"
                },
                {
                    "type": "goose_result *",
                    "name": "out_result"
                }
            ],
            "deprecated": true,
            "comment": {
                "description": "**DEPRECATION NOTICE**: This method is scheduled for removal in a future release.\n\nExecutes the prepared statement with the given bound parameters, and returns an optionally-streaming query result.\nTo determine if the resulting query was in fact streamed, use `goose_result_is_streaming`\n\nThis method can be called multiple times for each prepared statement, and the parameters can be modified\nbetween calls to this function.\n\nNote that the result must be freed with `goose_destroy_result`.\n\n",
                "param_comments": {
                    "prepared_statement": "The prepared statement to execute.",
                    "out_result": "The query result."
                },
                "return_value": "`GooseSuccess` on success or `GooseError` on failure."
            }
        }
    ]
}