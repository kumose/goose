{
  "description": "Run without the LocalFileSystem enabled.",
  "on_init": "SET disabled_filesystems = 'LocalFileSystem';",
  "skip_compiled": "true",
  "skip_error_messages": [
    "LocalFileSystem"
  ],
  "skip_tests": [
    {
      "reason": "TODO",
      "paths": [
        "tests/scripts/sql/attach/attach_filepath_roundtrip.test"
      ]
    }
  ]
}
